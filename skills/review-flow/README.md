# Review Flow Skill

Run local review with one command:

`/review`

What it guarantees:

1. Finds `HIGH` and `LOW` issues in changed files.
2. Blocks on `HIGH` findings.
3. Returns a concise review decision for release workflow.
