# Verify Flow Skill

Run verification in one command:

`/verify`

Default checks:

1. `npm run check:local`
2. `npm run build`
3. `npm run package`

Quick checks:

1. `/verify quick` -> `npm run check:local`
