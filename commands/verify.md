---
description: Compatibility shim for `/verify`. Delegates to `skills/verify-flow/SKILL.md`.
---

# /verify (Compatibility Shim)

Use `verify-flow` skill as the canonical implementation.

Default order:

1. `check:local`
2. `build`
3. `package`
