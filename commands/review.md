---
description: Compatibility shim for `/review`. Delegates to `skills/review-flow/SKILL.md`.
---

# /review (Compatibility Shim)

Use `review-flow` skill as the canonical implementation.

- Report findings in `HIGH` and `LOW` groups.
- Block merge on any `HIGH` finding.
