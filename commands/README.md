# Commands Compatibility Layer

This directory is compatibility-only.

- Canonical workflow logic lives in `skills/`.
- Command files here must be thin shims that delegate to skills.
- Do not duplicate or fork workflow logic in command files.
