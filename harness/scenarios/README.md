# Harness Scenarios

Describe scenario-level workflows to be executed by the harness.

Each scenario should eventually include:

- `schemaVersion` (for contract compatibility checks)
- Goal
- Inputs
- Expected output
- Pass/fail criteria
